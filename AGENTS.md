# プロジェクトの目的

このリポジトリは、2台の Linux ラップトップで分かれている既存設定を統合し、今後の正本となる dotfiles / NixOS / Home Manager 構成を Git 管理するための作業場所である。

対象となる既存環境は次の2つ。

- Arch Linux 環境の設定群
- NixOS 環境の設定群

それぞれには、今後も採用したい良い設定と、今回限りで破棄したい設定が混在している。既存設定を丸ごと移植するのではなく、棚卸し内容と判断メモをもとに取捨選択し、必要に応じて修正してから統合する。

最終的な目標は、単一の Git リポジトリを2台のPCで共有し、共通方針に基づいた統一環境で作業できるようにすることである。

## 基本方針

- NixOS + Home Manager を正とする。
- Nix flake を入口にし、NixOS module と Home Manager module で設定を管理する。
- 共通設定とホスト別設定を明確に分ける。
- 既存の Arch Linux / NixOS 設定は参考資料として扱い、無条件には取り込まない。
- 一時的な設定、壊れている設定、再現性を損なう手動導入物は原則として破棄または再作成する。
- 秘密鍵、APIキー、OAuth token、password、ログイン状態は平文で Git 管理しない。

## 判断の軸

設定を追加するときは、次を明確にする。

- どちらの既存環境から来た設定か。
- 共通設定にするのか、ホスト別設定にするのか、ユーザー別設定にするのか。
- そのまま採用するのか、修正して採用するのか、破棄するのか。
- 再現すべき設定ファイルなのか、バックアップすべき状態データなのか、再ログインや再生成でよいものなのか。
- 2台で共有して安全か、ホスト固有情報や秘密情報を含まないか。

## リポジトリ構造の意図

- `flake.nix`: 全体の入口。対象ホストの NixOS 構成を束ねる。
- `modules/nixos/`: 全ホストで共有する NixOS module。
- `hosts/<hostname>/`: ホスト名、boot、hardware、ディスク、GPU、電源管理などのホスト別設定。
- `home/common.nix`: ユーザー環境で共有する Home Manager 設定。
- `home/<username>/`: ユーザー別の Home Manager 設定。
- `inventories/`: 既存環境の棚卸し、判断材料、未確認事項、破棄/移行方針の記録。

## 作業時の注意

- 実装前に、作業を独立して実装・検証・レビューできる単位へ分割する。
- 1つの作業項目は原則として1つのコミットに対応させる。
- 既存設定を移植する場合は、棚卸しや判断メモと照合して採用理由を明確にする。
- 不明な設定や実機でしか確認できない内容は推測で埋めず、未確認事項として残す。
- secrets の値は記録しない。必要な場合も、パス、種類、移行方針だけを扱う。
- コミットは、対象範囲、変更概要、検証結果、コミットメッセージ案を提示し、ユーザーの明示的な承認を得てから行う。
